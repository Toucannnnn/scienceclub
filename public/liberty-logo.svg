<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 96 96" width="96" height="96" role="img" aria-label="School logo placeholder">
  <!--
    PLACEHOLDER — replace this file with the real Liberty High School logo.
    Keep the filename (public/liberty-logo.svg) and no code needs to change.
    If your logo is a PNG, save it as public/liberty-logo.png instead and
    update the two `src` strings in src/components/site-footer.tsx and
    src/app/(public)/page.tsx.
    Deliberately generic: this is a neutral monogram, not an imitation of the
    school's actual crest.
  -->
  <defs>
    <linearGradient id="placeholder-ring" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#4285f4" />
      <stop offset="45%" stop-color="#9b72cb" />
      <stop offset="100%" stop-color="#d96570" />
    </linearGradient>
  </defs>
  <circle cx="48" cy="48" r="44" fill="none" stroke="url(#placeholder-ring)" stroke-width="4" />
  <text
    x="48"
    y="48"
    text-anchor="middle"
    dominant-baseline="central"
    font-family="system-ui, -apple-system, 'Segoe UI', sans-serif"
    font-size="30"
    font-weight="600"
    fill="url(#placeholder-ring)"
  >LHS</text>
</svg>
